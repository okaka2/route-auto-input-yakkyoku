/**
 * 初回起動時にだけ用意する、初期設定のラベル(薬局版)。
 * 他のラベルは設定画面から自由に追加・削除できる(ここはあくまで最初の1回だけの
 * 初期値で、削除すれば消え、再起動しても復活しない)。
 *
 * 「患者」「薬局」は、業種特有の言葉として禁止語チェック(tests/wording.test.ts)の
 * 対象だったが、薬局版ではアプリ名・初期ラベルに使うため解禁している。
 */
export const DEFAULT_LABELS: readonly string[] = ['患者名', '個人宅', '施設'];
